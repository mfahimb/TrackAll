import 'package:flutter/material.dart';

class NPTEntryPage extends StatelessWidget {
  const NPTEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("NPT Entry")),
      body: const Center(
        child: Text("NPT Entry Form will go here"),
      ),
    );
  }
}
